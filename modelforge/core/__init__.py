"""（这个包是空的 —— 保留是为了记录一段设计史，以及一个没做出的决定。）

════════════════════════════════════════════════════════════════════════
M0 时期，这里曾经规划着四个模块
════════════════════════════════════════════════════════════════════════

    events.py    —— 事件类型定义（agent.thinking / tool.call / interrupt.request）
    state.py     —— 会话状态的数据模型
    machine.py   —— 状态机本体：拆题 → 选模 → 求解 → 验证 → 成文
    store.py     —— SQLite 持久化

当时把 `state.py` 标成了「必须最早定下来」，理由（写在这里的原话）是
「因为选定了『状态落盘、点击时重建』，这里的 schema 就是 checkpoint 的格式」。

**这个判断是对的。** M4 的第一件事确实是定 checkpoint 的 schema，
而且它决定了整个编排层的形状。

**但四个模块最后没有落在这个包里**，因为真正动手时发现它们不属于同一层：

    「事件类型」   →  providers/events.py（流事件） + sessions/base.py（日志事件）
    「状态模型」   →  sessions/base.py + sessions/project.py（投影）
    「持久化」     →  sessions/sqlite_store.py
    「状态机」     →  **还没写**

前三个在 M4 落进了 `modelforge/sessions/`。它们放一起是因为它们
**互相依赖得最紧**（类型、投影、存储是同一个设计的三面），
而 `core/` 这个名字太笼统，什么都能往里塞，反而不好。

════════════════════════════════════════════════════════════════════════
那「状态机」去哪了？
════════════════════════════════════════════════════════════════════════

**它没有被删除，只是还没决定要不要做。** 这是本文件留着的唯一理由。

原计划是「拆题 → 选模 → 求解 → 验证 → 成文」五个固定节点，每步强制中断。
但 M4 落地时选了另一条路：给模型一个 `ask_user` 工具，**由它自己决定何时问**
（见 docs/roadmap.md 的 ADR-010）。

两条路的区别是实质性的：

  · 硬编状态机 —— 流程可预期，不会漏问；但「这一步该问什么」变成
    写死在代码里的常量和提示词，而且必须先把整个流程设计对。
  · 工具驱动（现在这条）—— 灵活、实现量小、复用 M3 全部机制；
    代价是不保证模型在**每个**该问的地方都问。

M4 的实测结果是后者表现不错（它问的问题比预想的有信息量），
但那是**一次**观察，不是结论。

所以：**M5/M6 真正开始铺「一道题从输入到成文」的完整流程时再决定。**
到那时如果发现模型经常漏问关键决策点，正确的做法不是推倒重来 ——
状态机可以作为**约束**套在 `ask_user` 之上：固定节点强制中断，
节点之间的自由推理交给模型。两者不冲突。

在这个决定做出来之前，这个包保持为空。
"""
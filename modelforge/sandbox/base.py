"""代码执行沙箱的接口契约。

════════════════════════════════════════════════════════════════════════
为什么这里是一个「协议」而不是一个具体实现
════════════════════════════════════════════════════════════════════════
和 M1 的 `ChatProvider` 是同一个套路：把「做什么」和「怎么做」分开。

    ChatProvider    —— 换模型不改上层
    CodeExecutor    —— 换隔离方式不改上层

M3 只实现一种（子进程 + 独立 venv）。将来要换成 Docker、换成远程执行服务、
换成云函数，都只需要写一个新的类满足这个协议，Agent 循环一行不用改。

这不是为了「将来可能要用」而提前抽象 —— 而是因为**隔离强度本来就是一个
连续谱**，从「同一个解释器直接 exec」到「硬件级虚拟化」中间有十几个档位，
而不同部署环境（本地开发 / 学校服务器 / 云）的合理档位不一样。
把选择权留给部署者，是这个接口存在的唯一理由。

════════════════════════════════════════════════════════════════════════
安全边界的诚实说明（很重要，别自欺）
════════════════════════════════════════════════════════════════════════
M3 的实现是**子进程 + 独立 venv + 环境变量白名单 + 超时 + 独立工作目录**。
它挡得住什么、挡不住什么，必须写清楚：

  ✅ 挡得住：
     · 读走后端的 API Key（环境变量被换成白名单，根本没有那些变量）
     · 误删项目文件（工作目录是一个全新的临时目录，cwd 不在项目里）
     · 死循环卡死后端（超时后杀掉整个进程树）
     · 输出把上下文撑爆（输出截断）
     · GUI 弹窗（MPLBACKEND=Agg）

  ❌ 挡不住：
     · `open("C:/Users/xxx/文档/...")` 这样写绝对路径去读任意文件
     · `urllib.request.urlopen(...)` 发网络请求
     · 写绝对路径去覆盖别处的文件
     · 把 CPU 跑满（超时能杀掉，但杀之前那几秒是实打实占着的）

**这是「防误伤」级别，不是「防蓄意攻击」级别。**
对「用户自己跑自己模型的输出」这个场景，它是够用的 —— 因为写代码的和挨打的
是同一个人，模型也没有动机去攻击你。但如果哪天 ModelForge 要开放成多用户
的在线服务，这段实现必须整个换掉，换成容器或微虚拟机。

把这段写在这里而不是写在 README 里，是因为改这个文件的人一定会看到它。
"""

from __future__ import annotations

from typing import Protocol, runtime_checkable

from pydantic import BaseModel

__all__ = ["CodeExecutor", "ExecutionResult"]


class ExecutionResult(BaseModel):
    """一次代码执行的原始结果 —— 还没经过任何界面包装。

    注意这里**没有** `ok` 字段（虽然 `ToolResult` 事件里有）。
    两者的区别是职责不同：

      · 这里只回答「进程怎么退出的」——客观事实
      · `ToolResult.ok` 回答「这次工具调用算不算成功」——带着判断

    判断放在上层做，因为「成功」的定义会变：现在是非零退出码就算失败，
    将来可能加上「输出里含 Traceback 也算失败」。事实不用变，判断会变。
    """

    stdout: str = ""
    stderr: str = ""
    exit_code: int | None = None
    duration_ms: int = 0
    timed_out: bool = False
    # 沙箱本身没能把代码跑起来（解释器不存在、临时目录建不了……）。
    # 与「代码跑了但报错」是完全不同的两件事，绝不能混。
    error: str | None = None

    @property
    def launched(self) -> bool:
        """代码到底有没有被真正执行过。"""
        return self.error is None


@runtime_checkable
class CodeExecutor(Protocol):
    """所有代码执行后端都必须满足的契约。

    和 `ChatProvider` 一样刻意保持极小：只有一个方法。
    """

    name: str
    """执行器标识名，如 "subprocess"。用于日志和界面提示。"""

    async def run(self, code: str, *, timeout: float | None = None) -> ExecutionResult:
        """在一个受限环境里跑一段 Python 代码，把结果收集回来。

        实现者必须保证的契约：

          1. **不抛异常。** 一切失败都折叠进 `ExecutionResult.error`。
             理由和 Provider 层一样：调用方用 `await` 一句话就能拿到结果，
             不需要在外面包 try/except 去猜哪里会炸。
             唯一的例外是 `asyncio.CancelledError` —— 那是上层要停我们，
             必须让它继续往上传播。

          2. **超时后必须杀掉整个进程树。** 只杀直接子进程是不够的：
             代码里 `subprocess.Popen` 出来的孙子进程会活下来继续占资源，
             在 Windows 上还会一直占着管道句柄不放。

          3. **必须能安全地并发调用。** 每次执行都要用**互不相同的**工作目录，
             否则两次执行会互相踩对方的文件。

        Args:
            code: 要执行的 Python 源码。
            timeout: 覆盖默认超时（秒）。None 表示用配置里的值。
        """
        ...

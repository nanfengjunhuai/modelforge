"""沙箱执行层：在隔离环境中运行 LLM 生成的求解代码。

安全约束（M3 实现，但现在就要记住）：

1. **独立运行时**——沙箱使用自己的 Python 环境，而不是后端的 ``.venv``。
   同一个环境里跑不可信代码，等于把整个项目和密钥都交出去。
2. **资源限制**——CPU 时间、内存上限、墙钟超时。
3. **文件系统隔离**——只允许读写专用临时目录。
4. **不继承环境变量**——否则 ``os.environ["DEEPSEEK_API_KEY"]`` 就泄露了。

规划模块：
    runner.py   —— 启动隔离子进程、喂入代码、收集 stdout / 产物
    limits.py   —— 资源限制与超时策略
"""
